import React from "react"


/**
 * Currently a placeholder, the intention is to use this for non-movable elements inside 
 * the container, i.e. a link track in between every pair of tracks
 * 
 */

const NonDraggable = ({ children, id, index, className }) => {

    return (
        <div>

        </div>

    )
}

export default NonDraggable;